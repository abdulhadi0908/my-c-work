#include <stdio.h>

int main()
{
    // int a;
    // a =1;
    int a = 1; // we can write it like this also.

    // float b = 1.4;
    float b;
    b = 1.4; // same goes with this also.

    // char c = 'a';
    char c;
    c = 'a'; // we have to keep characters in single quote only.

   
    printf("Tthe value of a is %d\n", a);
    printf("Tthe value of a is %f\n", b);
    printf("Tthe value of a is %c\n", c);
    return 0;
}